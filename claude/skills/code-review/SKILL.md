---
name: code-review
description: "Review uncommitted changes using Codex CLI. Use when user wants a code review of current changes, staged files, or working directory modifications."
---

# Code Review Skill

Reviews uncommitted git changes using OpenAI Codex CLI.

## Usage

Run Codex CLI in review mode on the current working directory:

```bash
codex review --uncommitted
```

Requires Codex CLI to be installed.
